const number = 10;

const numberIsPair = (20%2) === 0; //If pair returns true [!]

if(number%2 === 0 && numberIsPair) {
    console.log("\nThe two numbers are pair\n");
} else {
    console.log("\nAny of two numbers is not pair\n");
}